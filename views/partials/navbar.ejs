<div class="navbar">
    <a href="/" class="navbar-brand">FileNook</a>
    <div class="navbar-links">
      <% if (user) { %>
        <a href="#">Welcome <%= user.username %></a> <!-- Show username if logged in -->
        <a href="/users/logout" class="login-btn">Logout</a>
      <% } else { %>
        <a href="/users/login" class="login-btn">Login</a>
        <a href="/users/register" class="register-btn">Register</a>
      <% } %>
    </div>
  </div>
  